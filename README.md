# ABC

## 江恩九方图网页版

见 [`gann-square/`](./gann-square/)。

**不需要下载到本地才能用**：这是纯静态页面，部署到任意静态托管后打开链接即可；若只要本机预览，打开 `gann-square/index.html`，或：

```bash
python3 -m http.server 8080
# 访问 http://localhost:8080/gann-square/
```
